import 'package:flutter/material.dart';

class FavoriteTeamPage extends StatelessWidget {
  const FavoriteTeamPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
        child: Column(
      children: [
        Container(
          height: 100,
          width: double.infinity,
          color: Colors.green,
        )
      ],
    ));
  }
}
